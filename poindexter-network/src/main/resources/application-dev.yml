spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/poindexter-network}
    username: ${DB_USERNAME:username}
    password: ${DB_PASSWORD:password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: ${EMAIL_HOST_NAME:smtp.gmail.com}
    port: 465
    username: ${EMAIL_USER_NAME:shopping.bartek@gmail.com}
    password: ${EMAIL_PASSWORD:nbzu rqta uoci uhsy}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            trust: "*"
          starttls:
            enable: true
    protocol: smtp
  config:
    import=optional:
       file:
         .env[.properties]
application:
  security:
    jwt:
      secret-key: eyJhbGciOiJIUzI1NiJ9eyJSb2xlIjoiQWRtaW4iLCJJc3N1ZXIiOiJJc3N1ZXIiLCJVc2VybmFtZSI6IkphdmFJblVzZSIsImV4cCI6MTcxOTkyNzIyMCwiaWF0IjoxNzE5OTI3MjIwfQYNJbf8TL9Wa9urtOJLoJvZIEO5cfBa0RDIfy8lAVuQ
      expiration: 86400000 # a day
      refresh-token:
        expiration: 604800000 # 7 days
  mailing:
    frontend:
      activation-url: http://localhost:8080/activate-account
  CORS:
    origins: http://localhost:4200,http://localhost:8080
  file:
    uploads:
      photos-output-path: ./uploads
server:
  port: 8089